import React from 'react'

import SvgLoader from "../../assets/svgs/loader";

// LOADER
const Loader = (props) => {
    return (
        <div 
            className="m-loader"
        >
            <SvgLoader />
        </div>
    );
}
  
export default Loader